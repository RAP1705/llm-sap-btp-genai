applications:
  - name: llm-demo-gen-ai-hub-batch3
    memory: 512M
    instances: 1
    buildpacks:
      - python_buildpack
    command: streamlit run chat-genaihub-openai.py --server.port ${PORT} --server.address 0.0.0.0